% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaitnorm-data.R
\name{loadGaitNorm}
\alias{loadGaitNorm}
\title{Load a normative gait reference dataset}
\usage{
loadGaitNorm(
  dataset = "adult_reference",
  cycle = c(101, 51),
  age = NULL,
  speed = NULL,
  features = TRUE,
  reload = FALSE
)
}
\arguments{
\item{dataset}{Dataset name; one of \code{\link[=listGaitNorms]{listGaitNorms()}}\verb{$dataset}
(default \code{"adult_reference"}).}

\item{cycle}{Gait-cycle length in points: \code{101} (default) or \code{51}.}

\item{age}{Optional age in years; if given it must fall within the dataset's
\code{age_min}/\code{age_max}, otherwise an error is raised (this reference set is not
stratified by age).}

\item{speed}{Optional walking-speed condition (e.g. \code{"self_selected"}); if
given it must match the dataset's \code{speed}.}

\item{features}{Logical; if \code{TRUE} (default) also load the normative feature
reference matrix (subjects x concatenated 51-point kinematics) used to build
the GDI basis.}

\item{reload}{Logical; force a re-read even if the dataset is cached.}
}
\value{
A \code{gait_norm} list with elements: \code{dataset}, \code{version},
\code{cycle_length}, \code{percent} (cycle sampling points), \code{variables},
\code{mean} and \code{sd} (each a \code{length(variables)} x \code{cycle_length} matrix, rows
named by variable, columns named by cycle percentage), \code{units},
\code{features} (the feature matrix or \code{NULL}), and \code{provenance} (a one-row
manifest data frame).
}
\description{
Loads mean and standard-deviation kinematic waveform bands over the gait
cycle for the nine Gait Deviation Index / Gait Profile Score kinematic
variables, and (optionally) the normative feature reference set used to
construct the Gait Deviation Index.
}
\examples{
norm <- loadGaitNorm("adult_reference", cycle = 101)
dim(norm$mean)
norm$variables
}
\references{
Schwartz MH, Rozumalski A (2008). "The Gait Deviation Index: a new
comprehensive index of gait pathology." Gait & Posture 28(3):351-357.
Baker R, et al. (2009). "The Gait Profile Score and Movement Analysis
Profile." Gait & Posture 30(3):265-269.
}
\seealso{
\code{\link[=listGaitNorms]{listGaitNorms()}}
}
